import { IFragment } from "./base-types";
/**
 * Contains the logic to get, create and delete geometric subsets of an IFC model. For example,
 * this can extract all the items in a specific IfcBuildingStorey and create a new Mesh.
 */
export declare class Blocks {
    private fragment;
    readonly ids: Set<number>;
    readonly visibleIds: Set<number>;
    get count(): number;
    private _visibilityInitialized;
    private _originalIndex;
    private _idIndexIndexMap;
    constructor(fragment: IFragment);
    setVisibility(visible: boolean, itemIDs?: Set<string>, isolate?: boolean): void;
    private initializeVisibility;
    dispose(): void;
}
