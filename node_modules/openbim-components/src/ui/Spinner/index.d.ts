import { SimpleUIComponent } from "../SimpleUIComponent";
import { Components } from "../../core";
export declare class Spinner extends SimpleUIComponent<HTMLDivElement> {
    name: string;
    constructor(components: Components);
}
