import * as THREE from "three";
export declare class BufferManager {
    geometry: THREE.BufferGeometry;
    /** Buffer increment when geometry size is exceeded, multiple of 3. */
    bufferIncrease: number;
    /**
     * The maximum capacity of the buffers. If exceeded by the {@link size},
     * the buffers will be rescaled.
     */
    capacity: number;
    /** The current size of the buffers. */
    get size(): number;
    get attributes(): THREE.BufferAttribute[];
    constructor(geometry: THREE.BufferGeometry);
    addAttribute(attribute: THREE.BufferAttribute): void;
    resetAttributes(): void;
    createAttribute(name: string): void;
    updateCount(size: number): void;
    resizeIfNeeded(increase: number): void;
    private resizeBuffers;
}
